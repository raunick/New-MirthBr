# =====================================
# MirthBR - Environment Variables Example
# =====================================
# Copy this file to .env and configure as needed

# ----- Backend -----
# Database connection (SQLite path inside container)
DATABASE_URL=sqlite:/app/data/mirth.db

# Server binding
BIND_ADDRESS=0.0.0.0
PORT=3001

# Logging level (trace, debug, info, warn, error)
RUST_LOG=info

# API Key for authentication (REQUIRED in production!)
# Generate with: openssl rand -base64 32
API_KEY=dev-key-change-in-production-32chars

# TLS Configuration (optional)
# TLS_CERT_PATH=/path/to/cert.pem
# TLS_KEY_PATH=/path/to/key.pem

# ----- Frontend -----
# Backend API URL (use container name in Docker)
NEXT_PUBLIC_API_URL=http://backend:3001
